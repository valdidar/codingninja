package recursion;

public interface Interferance {
	//we don't have to 	write public and abstract as they are already there
	//we can't inherit interface from anything else, we have to make it from scratch
	int pepepopo();
	String name();
}
