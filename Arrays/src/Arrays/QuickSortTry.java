package Arrays;
public class QuickSortTry {
	private static void quickSortTry(int[] arr, int low, int high) {
		System.out.println("arr starts from "+low+" "+high);
		int pivot=arr[]
	}
	public static void main(String[] args) {
		int arr[]=InputAndPrintArray.inputArray();
		int low=0;
		int high=arr.length-1;
		quickSortTry(arr,low,high);
	}

}
